[project]
name = "deepseek-browser-cli"
version = "0.1.0"
description = "Browser automation for chat.deepseek.com via CDP — with MCP server, agent bridge, and programmatic API"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "zhaozhoubin" },
]
keywords = [
    "deepseek",
    "browser-automation",
    "cdp",
    "mcp",
    "ai-agent",
    "chatbot",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "mcp>=1.27.0",
]

[project.scripts]
deepseek-browser = "deepseek_browser_cli.cli:main"
deepseek-agent = "deepseek_browser_cli.agent_cli:main"
deepseek-mcp = "deepseek_browser_cli.mcp_server:main"

[project.urls]
Homepage = "https://github.com/zhaozhoubin/deepseek-web-cli"
Repository = "https://github.com/zhaozhoubin/deepseek-web-cli"
Issues = "https://github.com/zhaozhoubin/deepseek-web-cli/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
